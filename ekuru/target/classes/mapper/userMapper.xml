<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.scit.ekuru.dao.UserMapper">

<insert id="insertUser" parameterType="user">
	insert into e_user_tb(userId, userPw, userNm, userType) values(#{userId}, #{userPw}, #{userNm}, #{userType})
</insert>

<select id="loginUser" parameterType="user" resultType="user">
	select userid, userpw, usernm, useraddr, userzip, userphone, userconfirm, usertype, userpoint
	from e_user_tb
	where userId = #{userId} and userPw = #{userPw}
</select>

<select id="selectPoint" parameterType="user" resultType="chargepoint">
	select pointChargeNum, userId, pointprodnum, to_char(pointChargedIndate, 'YYYY-MM-DD') pointchargedindate
	from e_point_tb
	where userId like #{userId}
	order by pointChargeNum
</select>


</mapper>
